---
import ThemeChooser from './ThemeChooser.astro';

const base = import.meta.env.BASE_URL;
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const links = [
  { href: `${base}`, label: 'Home', match: (p: string) => p === '' || p === '/' || p === base.replace(/\/$/, '') },
  { href: `${base}docs/`, label: 'Docs', match: (p: string) => p.includes('/docs') },
  { href: 'https://github.com/fetchurl', label: 'GitHub', external: true },
];
---

<header class="border-b border-base-300 bg-base-100">
  <div class="navbar mx-auto max-w-6xl px-4">
    <div class="navbar-start gap-2">
      <a href={base} class="flex items-center gap-2.5 font-semibold tracking-tight text-base-content no-underline">
        <span class="inline-flex size-8 items-center justify-center rounded-field bg-primary text-primary-content text-sm font-bold" aria-hidden="true">f</span>
        <span>fetchurl</span>
      </a>
    </div>
    <div class="navbar-center hidden md:flex">
      <nav class="menu menu-horizontal gap-1 px-1" aria-label="Primary">
        {
          links.map((link) =>
            link.external ? (
              <li>
                <a href={link.href} target="_blank" rel="noopener noreferrer" class="font-medium">
                  {link.label}
                </a>
              </li>
            ) : (
              <li>
                <a
                  href={link.href}
                  class:list={['font-medium', link.match(path) && 'bg-base-200']}
                  aria-current={link.match(path) ? 'page' : undefined}
                >
                  {link.label}
                </a>
              </li>
            ),
          )
        }
      </nav>
    </div>
    <div class="navbar-end gap-1">
      <ThemeChooser />
      <div class="dropdown dropdown-end md:hidden">
        <div tabindex="0" role="button" class="btn btn-ghost btn-sm" aria-label="Menu">
          Menu
        </div>
        <ul tabindex="0" class="dropdown-content menu bg-base-100 border border-base-300 rounded-box z-50 mt-2 w-48 p-2 shadow-none">
          {
            links.map((link) => (
              <li>
                {link.external ? (
                  <a href={link.href} target="_blank" rel="noopener noreferrer">
                    {link.label}
                  </a>
                ) : (
                  <a href={link.href} aria-current={link.match(path) ? 'page' : undefined}>
                    {link.label}
                  </a>
                )}
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</header>
